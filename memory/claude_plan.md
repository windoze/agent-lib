# 执行计划

## 范围

- 以 `TODO.md` 为唯一任务排序来源，识别第一个标题未带 `[DONE]` 的任务。
- 只完成该第一个未完成任务；完成、验证、记录、提交后停止。
- 若发现当前任务被具体前置问题阻塞，则在 `TODO.md` 插入最小必要前置任务，提交后停止。

## 步骤

1. 读取 `TODO.md`，确认第一个未完成任务及其验证要求。
2. 检查最近提交是否明确提到与该任务直接相关的未完成问题。
3. 阅读相关代码和文档，确定最小正确实现范围。
4. 实现当前任务；必要时补充或调整测试。
5. 运行格式化、lint 和任务要求的验证；若发现未排期失败，修复或在 `TODO.md` 中排入必要前置任务。
6. 将任务标题标记为 `[DONE]` 并更新完成记录。
7. 检查工作区差异，提交本次任务的全部相关改动。

## 进度

- 已创建初始执行计划。
- 已读取 `TODO.md`，确认第一个未完成任务是 `M5-7 [TODO] M5 review：facade 承诺收口`。
- 最近提交为 M5-6 相关进度/校验提交，未发现提交标题中明确声明需在 M5-7 前处理的额外未完成问题。
- 当前执行范围限定为 M5 review 收口：核对 M5 条目标注、重点回归点、`docs/facade-api.md` 同步情况，运行全量门禁，更新 `TODO.md` 并提交后停止。
- 已抽查 `docs/review-2026-07.md` 与 `docs/facade-api.md`：M5 条目均有对应标注或文档说明，`M-PROM-2` 的 budget 部分仍按任务单留在 M6-2，不阻塞 M5-7。
- 下一步执行全量门禁；若出现未排期失败，将先修复或在 `TODO.md` 中排入必要前置任务。
- 门禁已通过：`cargo fmt --all`、默认 clippy、external features clippy、`cargo test -p agent-lib --lib facade::`、`cargo test --all --all-targets`、rustdoc。
- 已将 `M5-7` 标记为 `[DONE]`，并在 `TODO.md` 写入 review 完成记录。下一步检查 diff 并提交本任务改动。
