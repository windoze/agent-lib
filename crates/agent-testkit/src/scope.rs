//! The `TestScope` builder over `agent-lib`'s `HandlerScope`, wiring per-family
//! handlers without hiding `UnhandledRequirement`. Implemented in milestone
//! M2-3.
//!
//! This module is an intentional skeleton stub for the crate topology; it is
//! filled in by a later milestone. See [`crate`] for the full module map.
