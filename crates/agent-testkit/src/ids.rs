//! Deterministic id sources for tests: `SeqIds` plus `RequirementIds` and
//! `ToolExecutionIds` implementations that stay unique across
//! parent/child/subagent/trace nodes. Implemented in milestone M1-2.
//!
//! This module is an intentional skeleton stub for the crate topology; it is
//! filled in by a later milestone. See [`crate`] for the full module map.
